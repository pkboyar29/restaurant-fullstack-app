import { MenuSection } from "./MenuSection"

// TData
export type MenuPosition = {
   id: number
   name: string
   descr: string
   section: MenuSection
   price: number
   portion: string
   image1: string
   image2: string
   image3: string
   image4: string
}